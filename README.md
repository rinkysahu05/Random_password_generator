# Task_1
# Skills_Updates
# Random_password_generator
import random
import string
def generate_password(length):
characters = String.ascii_letters + String.numbers + string.symbol
password = " ".join(random.choice(characters) for_in range(length))
return password
# Usage Example:
password = generate_password()
print(password)
